{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "copy py files to build",
            "type": "shell",
            "command": "mkdir -p .aws-sam/build/CalculatorFunction & cp *.py .aws-sam/build/CalculatorFunction/",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }       
    ]
}